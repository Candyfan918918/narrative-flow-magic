/* Pixel-perfect port of project/Welcome.dc.html — served verbatim. */
export function WelcomePage() {
  return (
    <iframe
      src="/shutap/Welcome.dc.html"
      title="Shutap — Welcome"
      style={{ position: 'fixed', inset: 0, width: '100%', height: '100%', border: 0, background: '#fdf0f5' }}
    />
  )
}
